<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csu.dao.mapper.AccountMapper">

    <resultMap id="accountResultMap" type="com.csu.model.Entity.Account">
        <id property="username" column="userid"/>
        <result property="email" column="email"/>
        <result property="firstName" column="firstname"/>
        <result property="lastName" column="lastname"/>
        <result property="status" column="status"/>
        <result property="address1" column="addr1"/>
        <result property="address2" column="addr2"/>
        <result property="city" column="city"/>
        <result property="state" column="state"/>
        <result property="zip" column="zip"/>
        <result property="country" column="country"/>
        <result property="phone" column="phone"/>
    </resultMap>

    <select id="selectByUsername" resultMap="accountResultMap">
        SELECT * FROM account WHERE userid = #{username}
    </select>

    <select id="searchAccounts" resultMap="accountResultMap">
        SELECT * FROM account
        WHERE LOWER(firstname) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(lastname) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(email) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
        ORDER BY userid
    </select>

    <select id="selectAllAccounts" resultMap="accountResultMap">
        SELECT * FROM account
        ORDER BY userid
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAllAccounts" resultType="int">
        SELECT COUNT(*) FROM account
    </select>

    <update id="updateAccountStatus">
        UPDATE account SET status = #{status} WHERE userid = #{username}
    </update>

</mapper>
